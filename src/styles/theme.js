const theme = {
  flexBox: (align = '', justify = '') => `
  display: flex;
  align-items: ${align};
  justify-content: ${justify};
`,
  maxWidth: '1256px',
  margin: '110px auto 0px',
  padding: '0 60px',
  white: '#FFFFFF',
  Green: '#AFD485',
};

export default theme;
